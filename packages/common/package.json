{
  "name": "@rt-potion/common",
  "version": "1.0.0",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "test": "jest --silent",
    "format": "pnpm prettier --config ../../.prettierrc 'src/**/*.ts' --write",
    "lint": "pnpm eslint src/",
    "build": "pnpm tsc --project tsconfig.json",
    "clean": "rimraf dist"
  },
  "description": "Shared files between projects",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@rt-potion/common": "file:",
    "typescript": "^5.7.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "prettier": "^3.4.2",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.2.5"
  }
}
